# WCAG Scanner backend.
# On Cloudflare Pages it is served same-origin at /api/wcag-scan — no config needed.
# Set this ONLY to point local `vite dev` at a remote/preview Functions URL:
# VITE_WCAG_WORKER_URL=https://<preview>.pages.dev/api/wcag-scan
